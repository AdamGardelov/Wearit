interface:
  display_name: "Import Clothes"
  short_description: "Prepare private garment cutout bundles"
  default_prompt: "Use $import-clothes to turn my purpose-shot garment photos into an accepted cutout bundle for Wearit's Admin import."

policy:
  allow_implicit_invocation: true
